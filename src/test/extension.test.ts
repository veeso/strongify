// import * as assert from 'assert';
import * as vscode from 'vscode';

// import * as strongify from '../extension';

suite('Extension Test Suite', () => {
  vscode.window.showInformationMessage('Start all tests.');
});
